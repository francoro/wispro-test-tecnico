import { makeStyles } from '@material-ui/core/styles';
export const useStyles = makeStyles((theme) => ({
    paper: {
      height: 300,
      width: 400,
      backgroundColor: '#FFF',
      padding: '50px',
      top: '200px',
    },
  }));